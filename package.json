{
  "name": "openheader",
  "version": "1.2.1",
  "description": "A lightweight open-source extension to modify request and response headers and filter by URL regexes.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js chrome",
    "build:firefox": "node build.js firefox",
    "zip:chrome": "npm run build:chrome && cd dist/chrome && zip -r -FS ../openheader-chrome.zip .",
    "zip:firefox": "npm run build:firefox && cd dist/firefox && zip -r -FS ../openheader-firefox.zip .",
    "zip": "npm run zip:chrome && npm run zip:firefox"
  }
}
